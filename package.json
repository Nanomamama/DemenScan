{
  "name": "demenscan",
  "version": "1.0.0",
  "private": true,
  "description": "DemenScan frontend (no backend)",
  "scripts": {
    "start": "serve -s . -l $PORT"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serve": "^14.1.2"
  }
}
